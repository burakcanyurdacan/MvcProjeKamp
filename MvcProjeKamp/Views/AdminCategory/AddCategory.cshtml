@model EntityLayer.Concrete.Category

@{
    ViewBag.Title = "AddCategory";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h1>Yeni Kategori Ekleme Sayfası</h1>
<br />

<!-- HTML HELPER KULLANMADAN YAPILAN EKLEME FORMU -->
@*<form class="form-group" method="post">
        <input type="text" placeholder="Kategori Adı" class="form-control" name="CategoryName" />
        <br />
        <input type="text" placeholder="Açıklama" class="form-control" name="CategoryDesc" />
        <br />
        <button class="btn btn-primary">Ekle</button>
    </form>*@

@using (Html.BeginForm("AddCategory", "AdminCategory", FormMethod.Post))
{
    <div class="form-group">
        @Html.Label("Kategori Adı")
        @Html.TextBoxFor(x => x.CategoryName,new { @class="form-control" })
        @Html.ValidationMessageFor(X => X.CategoryName)
        <br /><br />
        @Html.Label("Açıklama")
        @Html.TextAreaFor(x => x.CategoryDesc,6,1, new { @class = "form-control" })
        @Html.ValidationMessageFor(X => X.CategoryDesc)
    </div>
    <div class="form-group">
        <button class="btn btn-success">Kaydet</button>
    </div>
}